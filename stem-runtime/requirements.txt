# Torch and torchaudio are installed separately by build.py so the base package
# stays MPS-capable on macOS and CPU-only on Windows/Linux.
numpy<2
setuptools==80.9.0
demucs==4.0.1
beartype==0.14.1
einops==0.6.1
rotary-embedding-torch==0.3.5
packaging==25.0
